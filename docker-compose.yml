version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mount topic images to allow persistence/updating without rebuild
      - ./public/topic_images:/app/public/topic_images
    restart: unless-stopped
